{
  "name": "offer-lens",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI-powered job offer analyzer. Paste your offer, know if it's fair.",
  "scripts": {
    "lint:css": "stylelint css/*.css",
    "lint:js": "eslint js/*.js",
    "lint:html": "html-validate index.html",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.css": [
      "stylelint --fix",
      "prettier --write"
    ],
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.html": [
      "html-validate",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "html-validate": "^11.5.3",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.8.4",
    "stylelint": "^17.13.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-order": "^8.1.1"
  }
}
